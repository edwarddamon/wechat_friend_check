# 微信好友状态检测  |  客户端 + CLI

# === 主程序（wx_check.py / gui.py / show_wechat.py / auto_scrape.py） ===
wxauto4>=4.1            # 微信 4.x UI 自动化（免费版，支持微信 4.1.8.107）
pywin32>=306            # 把微信主窗口从托盘唤起（win32gui）+ COM 初始化（pythoncom）
# uiautomation 由 wxauto4 自动附带，用于 auto_scrape 读通讯录控件

# === 打包成 exe（build_exe.py） ===
pyinstaller>=6.0
